<UserControl x:Class="amira_kenza_yasmineUA2.AjouterAdherents"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <StackPanel Margin="10">
            <TextBlock Text="Ajouter un Adhérent" FontSize="14" FontWeight="Bold" Margin="0,0,0,10" Width="600" Foreground="#FFE9DDDD"/>
            <TextBlock Text="ID" Foreground="#FFE9DDDD" Width="280" FontWeight="Bold" />
            <TextBox x:Name="IDTextBox" Width="200" Margin="0,0,0,10"/>
            <TextBlock Text="Nom" Foreground="#FFE9DDDD" Width="280" FontWeight="Bold" />
            <TextBox x:Name="NomTextBox" Width="200" Margin="0,0,0,10"/>
            <TextBlock Text="Prénom" Foreground="#FFE9DDDD" Width="280" FontWeight="Bold" />
            <TextBox x:Name="PrenomTextBox" Width="200" Margin="0,0,0,10"/>
            <TextBlock Text="Email" Foreground="#FFE9DDDD" Width="275" FontWeight="Bold" />
            <TextBox x:Name="EmailTextBox" Width="200" Margin="0,0,0,10"/>
            <Button Content="Ajouter" Width="80" Click="AjouterAdherent_Click" Background="#FF2F316F" Foreground="Azure"/>
        </StackPanel>
    </Grid>
</UserControl>
